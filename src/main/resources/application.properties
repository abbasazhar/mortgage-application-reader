spring.application.name=mortgage-application-reader

server.port=8080

# Local storage: a JSON file on disk, no external database.
app.storage.file=./data/applications.json

# Public data source used to enrich a submitted postcode (free, no API key).
app.public-data.postcode-api-base-url=https://api.postcodes.io
